apiVersion: v1
kind: Service
metadata:
  name: {{ include "spoolman-ng.fullname" . }}
  labels:
    {{- include "spoolman-ng.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spoolman-ng.selectorLabels" . | nindent 4 }}
